<nav class="header-nav">
    <a href="{{ path('main_home') }}" title="Home">Home</a>
    <a href="{{ path('wish_list') }}" title="All Wish">All Wish</a>
    {% if app.user %}
        <a href="{{ path('wish_create') }}" title="Create">Create</a>
        <a href="{{ path('app_logout') }}" title="Logout user">Logout</a>
    {% else %}
        <a href="{{ path('app_login') }}" title="Login">Login</a>
        <a href="{{ path('app_register') }}" title="Register">Register</a>
    {% endif %}
    <a href="{{ path('main_aboutUs') }}" title="About Us">About Us</a>

</nav>
{% if app.user %}
<p> Bienvenue {{ app.user.email }}</p>
{% endif %}