{% extends 'base.html.twig' %}

{% block title %}
    {{ parent()}} | Create
{% endblock %}

{% block body %}
    <div class="container">
        {{ form_start(wishForm)}}
        {{ form_widget(wishForm) }}
        <button>Create!</button>
        {{ form_end(wishForm)}}
    </div>
{% endblock %}

