{% extends 'base.html.twig' %}

{% block title %}
    {{ parent()}} | Detail
{% endblock %}

{% block body %}
    <div class="container">
        <h2>{{ wish.title }}</h2>
        <p>Author : {{ wish.author }}</p>
        <p>Created : {{ wish.dateCreated | date('d/m/Y') }}</p>
        <p>Description : {{ wish.description ? wish.description : "no description" }}</p>
    </div>
{% endblock %}

